# Copy to .env and fill in what you need. Nothing here is required to run
# Kronagent in dry-run with sample data — every credential below degrades
# gracefully if omitted (see kronagent/config.py and kronagent/llm.py).

# LLM-enriched triage, threat-intel, correlation, and incident commander.
# Omit this and the pipeline falls back to deterministic triage only.
GEMINI_API_KEY=

# AWS — only needed for live SQS ingestion or real containment execution.
# AWS_REGION=us-east-1

# REQUIRED to connect a customer AWS account: the 12-digit account Kronagent
# itself runs in. The CloudFormation template embeds it in the customer's trust
# policy, so without it the template endpoint refuses rather than issuing a role
# nobody can assume.
# KRONAGENT_AWS_ACCOUNT_ID=

# How often to poll GuardDuty through a connected account's role (default 60).
# KRONAGENT_GUARDDUTY_POLL_SECONDS=60

# Kubernetes — only needed for real containment execution against a cluster.
# KRONAGENT_KUBECONFIG=/path/to/kubeconfig
# KRONAGENT_KUBE_CONTEXT=

# ChatOps — approval cards and allowlist expiry warnings. Without these the
# warnings still run and are still audited; they just aren't delivered anywhere.
# KRONAGENT_SLACK_BOT_TOKEN=
# KRONAGENT_SLACK_CHANNEL_ID=
# Maps Slack user id -> Kronagent operator id, so an entry's owner gets @-mentioned.
# KRONAGENT_SLACK_USER_MAPPING={"U12345":"dana"}

# How far ahead `promote.py warn-expiring` warns an owner that their
# auto-execute allowlist entry is about to lapse. Notice only — the entry
# expires on schedule whether or not anyone was told.
# KRONAGENT_ALLOWLIST_WARN_WITHIN=14d

# See README.md "Going live against real infrastructure" for the full list
# of safety-critical settings (KRONAGENT_DRY_RUN, KRONAGENT_KILL_SWITCH, etc.).
